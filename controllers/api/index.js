const router = require('express').Router();
const Routes = require('./Routes'); //add routes name 
const Routes = require('./Routes');//add routes name 

router.use('/', Routes); //add model name, route name 
router.use('/', Routes); //add model name, route name 

module.exports = router;
